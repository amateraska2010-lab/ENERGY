Как работает алгоритм SkillMatch AIСервис не просто ищет совпадения слов, а понимает смысл (семантику) текста с помощью технологий искусственного интеллекта. Процесс разбит на три этапа:1. Векторизация текста (Embeddings)В основе лежит модель Sentence-Transformers (на архитектуре BERT).Каждый текст (резюме студента или вакансия работодателя) переводится в многомерный вектор — массив чисел, который представляет собой «цифровой отпечаток» смысла.В этом векторном пространстве похожие навыки (например, «машинное обучение» и «Machine Learning») будут находиться рядом, даже если они написаны по-разному.2. Расчет соответствия (Cosine Similarity)Для определения того, насколько кандидат подходит вакансии, используется формула косинусного сходства:$$Cosine\_Similarity = \frac{\mathbf{A} \cdot \mathbf{B}}{\|\mathbf{A}\| \|\mathbf{B}\|}$$Значение 1.0 (100%) означает идеальное совпадение векторов.Сервис сравнивает вектор студента с векторами всех вакансий в базе и выдает результат в процентах.3. Анализ разрывов (Gap Analysis)Система сравнивает ключевые токены (навыки) из вакансии с профилем студента:Если в вакансии есть технология (например, Docker), а в резюме её нет, AI идентифицирует это как «недостающий навык».На основе этого формируются автоматические рекомендации: какой курс пройти или какой проект добавить в портфолио.🛠 Архитектура решения (MVP)Frontend (Streamlit): Интерфейс, где студент вводит данные, а работодатель видит аналитику.API (FastAPI): Сервер, который принимает запросы и связывает ИИ-логику с базой данных.NLP Core: Модель all-MiniLM-L6-v2, которая обеспечивает высокую скорость работы прямо на локальном компьютере или сервере (не требует дорогого GPU).📈 Почему это важно для города (согласно целям Хакатона)Для студентов: Сокращает время на поиск первой работы и помогает понять требования рынка труда.Для колледжей: Позволяет видеть, каких навыков не хватает студентам, и адаптировать учебные программы.
